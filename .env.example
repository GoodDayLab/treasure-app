DATABASE_URL="postgresql://user:password@localhost:5432/treasure?schema=public"

# 部署到 Vercel 並在 Storage 分頁建立 Blob store 之後,這個變數會自動注入,
# 本機開發如果要測照片上傳,才需要手動去 Vercel 專案設定裡複製貼上。
BLOB_READ_WRITE_TOKEN=""
